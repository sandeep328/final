import { FiltertypesPipe } from './filtertypes.pipe';

describe('FiltertypesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertypesPipe();
    expect(pipe).toBeTruthy();
  });
});
